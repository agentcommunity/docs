{
  "github": {
    "enabled": false
  },
  "builds": [
    {
      "src": "mkdocs.yml",
      "use": "@vercel/static-build",
      "config": {
        "installCommand": "pip install -r requirements.txt",
        "buildCommand": "git clone --depth 1 https://github.com/agentcommunity/AgentInterfaceDiscovery tmp-spec-repo && mv tmp-spec-repo/docs/aid docs/aid && mkdocs build --site-dir public"
      }
    }
  ]
}